# คู่มือเพิ่มเนื้อหาและข้อสอบ

repo นี้คือแหล่งเนื้อหาของแอป **KnowledgeQuizApp** — แอปอ่าน `manifest.json` ที่ root แล้วดาวน์โหลดไฟล์ `.enc` ไปถอดรหัสในเครื่อง

```
https://raw.githubusercontent.com/Linlijian/content-hub/main/manifest.json
```

---

## สารบัญ

1. [ภาพรวม](#1-ภาพรวม)
2. [เตรียมเครื่องครั้งแรก](#2-เตรียมเครื่องครั้งแรก)
3. [เพิ่มหนังสือ/บทเรียน](#3-เพิ่มหนังสือบทเรียน)
4. [เพิ่มข้อสอบ](#4-เพิ่มข้อสอบ)
5. [เพิ่มเส้นทางตามตำแหน่งงาน](#5-เพิ่มเส้นทางตามตำแหน่งงาน)
6. [เข้ารหัสและเผยแพร่](#6-เข้ารหัสและเผยแพร่)
7. [ตรวจสอบก่อน push](#7-ตรวจสอบก่อน-push)
8. [กฎที่ห้ามพลาด](#8-กฎที่ห้ามพลาด)
9. [แก้ปัญหา](#9-แก้ปัญหา)

---

## 1. ภาพรวม

```
content-hub/
├─ manifest.json          ← ไม่เข้ารหัส  แอปอ่านไฟล์นี้ก่อน (เครื่องมือสร้างให้)
├─ x1.enc  x2.enc  ...    ← บทเรียน/หนังสือ   (ต้นฉบับ x1.json x2.json)
├─ e1.enc  e2.enc  ...    ← ข้อสอบ             (ต้นฉบับ e1.json e2.json)
├─ book/*.pdf.enc         ← ไฟล์ PDF หนังสือ
└─ image/*.png.enc        ← รูปในโจทย์/ตัวเลือก
```

**สิ่งที่ขึ้น GitHub มีแค่ `.enc` กับ `manifest.json`** — ไฟล์ต้นฉบับ `.json` `.pdf` `.png` อยู่ในเครื่องคุณเท่านั้น
(`.gitignore` ของ repo นี้กันไว้แล้ว ต่อให้วางไว้ในโฟลเดอร์เดียวกันก็ไม่หลุดขึ้นไป)

### แอปทำอะไรตอนผู้ใช้กด "ตรวจหาเนื้อหาใหม่"

1. อ่าน `manifest.json` → ถ้า `version` **ต่างจาก**ที่เคยโหลด = มีของใหม่
2. ดูไฟล์ใน `files[]` ทีละไฟล์ → **ไฟล์ไหน sha256 เท่าเดิมจะข้าม** (โหลดเฉพาะไฟล์ที่แก้)
3. ดาวน์โหลด → ตรวจ sha256 → ถอดรหัส → นำเข้าฐานข้อมูลตาม `id` (มีอยู่แล้ว = ทับ, ยังไม่มี = เพิ่ม)
4. PDF/รูปที่อ้างถึงจะถูกโหลดและถอดรหัสเก็บในเครื่อง
5. **คะแนน/ประวัติการทำข้อสอบของผู้ใช้ไม่ถูกแตะ**

> ผลที่ตามมา: แบ่งไฟล์ให้เล็ก = ผู้ใช้โหลดน้อยลงเวลาอัปเดต เช่น ข้อสอบ 1 ไฟล์ต่อ 1 หนังสือ

---

## 2. เตรียมเครื่องครั้งแรก

| ต้องมี | ใช้ทำอะไร |
|---|---|
| `KnowledgeQuizApp\tools\QuizContentEncryptor.exe` | เข้ารหัส + สร้าง manifest + push |
| clone repo นี้ไว้ที่ `D:\claude.ai\content-hub` และล็อกอิน git แล้ว | ให้ปุ่ม commit + push ทำงาน |
| **รหัสผ่านเนื้อหา** | ต้องตรงกับ `ContentPassword` ใน `KnowledgeQuizApp\App\MauiProgram.cs` |

แนะนำให้เก็บไฟล์ต้นฉบับไว้ในโฟลเดอร์นี้เลย (ถูก gitignore อยู่แล้ว):

```
D:\claude.ai\content-hub\
├─ x1.json  e1.json ...          ← ต้นฉบับ (ไม่ขึ้น git)
├─ _source\book\*.pdf            ← PDF ต้นฉบับ (ไม่ขึ้น git)
└─ _source\image\*.png           ← รูปต้นฉบับ (ไม่ขึ้น git)
```

> ⚠️ **สำรองไฟล์ต้นฉบับไว้ที่อื่นด้วย** (Google Drive ฯลฯ) — มันไม่อยู่บน GitHub ถ้าเครื่องหายคือหาย
> กู้กลับได้จาก `.enc` ด้วยปุ่ม "ถอดรหัสแล้วบันทึกไฟล์" แต่ต้องจำรหัสได้

---

## 3. เพิ่มหนังสือ/บทเรียน

### 3.1 เตรียม PDF (ถ้ามี)

1. เปิด `QuizContentEncryptor.exe` → ใส่รหัสผ่าน
2. **+ เลือกไฟล์ (PDF/รูป/JSON)** → เลือก PDF
3. **โฟลเดอร์ปลายทาง** → `D:\claude.ai\content-hub\book`
4. **เข้ารหัส (+ตรวจย้อนกลับ)** → ได้ `ชื่อไฟล์.pdf.enc` (log ต้องขึ้น `OK ... ถอดกลับได้ตรงทุกไบต์`)

> ไฟล์ .enc ใหญ่กว่าต้นฉบับ ~33% (เข้ารหัสเป็น base64) — GitHub รับได้ไฟล์ละไม่เกิน 100 MB
> ดังนั้น PDF ต้นฉบับไม่ควรเกิน ~70 MB

### 3.2 เขียนไฟล์บทเรียน เช่น `x3.json`

```json
{
  "books": [
    {
      "id": "civil-procedure-2477",
      "title": "ประมวลกฎหมายวิธีพิจารณาความแพ่ง",
      "author": "สำนักงานศาลยุติธรรม",
      "category": "กฎหมาย",
      "pdfFileName": "book/ประมวลกฎหมายวิธีพิจารณาความแพ่ง.pdf.enc",
      "coverImageFileName": null,
      "chapters": [
        {
          "id": "cp-ch1",
          "orderIndex": 1,
          "title": "ภาค 1 บททั่วไป",
          "summaryMarkdown": "สรุปสาระสำคัญของภาคนี้..."
        },
        {
          "id": "cp-ch2",
          "orderIndex": 2,
          "title": "ภาค 2 วิธีพิจารณาในศาลชั้นต้น",
          "summaryMarkdown": "..."
        }
      ]
    }
  ]
}
```

| ฟิลด์ | จำเป็น | คำอธิบาย |
|---|:-:|---|
| `id` | ✅ | ภาษาอังกฤษ ตัวเล็ก ขีดกลาง ห้ามซ้ำกับเล่มอื่น **ห้ามเปลี่ยนหลังเผยแพร่** |
| `title` | ✅ | ชื่อที่แสดงในแอป |
| `author` | ✅ | ผู้แต่ง/หน่วยงาน |
| `category` | ✅ | **แสดงเป็นปุ่มกรองในหน้าหนังสือตามตัวอักษรนี้เป๊ะ** — ใช้ชื่อเดิมให้ตรงกันทุกเล่ม (เช่น `กฎหมาย` ไม่ใช่ `กฎหมาย ` หรือ `law`) |
| `pdfFileName` | – | path ของ `.pdf.enc` นับจาก root repo — ใส่ `null` ถ้าไม่มี |
| `coverImageFileName` | – | รูปปก (`image/xxx.png.enc`) แอปทำ thumbnail ให้เอง — ใส่ `null` ได้ |
| `chapters[].id` | ✅ | ห้ามซ้ำทั้ง repo (ข้อสอบกับเส้นทางตำแหน่งงานอ้างถึงตัวนี้) |
| `chapters[].orderIndex` | ✅ | ลำดับที่แสดง เริ่ม 1 |
| `chapters[].summaryMarkdown` | ✅ | สรุปรายบท — ⚠️ **หน้าอ่านสรุปแสดงเป็นข้อความธรรมดา** `**ตัวหนา**` และ `$สูตร$` จะขึ้นเป็นเครื่องหมายดิบ ใช้ขึ้นบรรทัดใหม่ (`\n`) กับ `-` แทน |

**ไฟล์ละกี่เล่มก็ได้** แต่แนะนำ 1 ไฟล์ต่อ 1 หมวด จะได้แก้ทีละส่วนโดยผู้ใช้ไม่ต้องโหลดใหม่ทั้งหมด

---

## 4. เพิ่มข้อสอบ

### 4.1 เตรียมรูป (ถ้าข้อสอบมีรูป)

เหมือนข้อ 3.1 แต่ปลายทางเป็น `D:\claude.ai\content-hub\image` → ได้ `ชื่อรูป.png.enc`

- ใช้ **PNG หรือ JPG**
- กว้างไม่เกิน ~1200px พอ (ใหญ่กว่านี้เปลืองเน็ตผู้ใช้โดยไม่ได้ชัดขึ้น)
- **ชื่อไฟล์ห้ามซ้ำกันภายในชุดข้อสอบเดียวกัน** — แอปเก็บรูปตามชื่อไฟล์แยกโฟลเดอร์ต่อชุด ถ้าชื่อซ้ำจะได้รูปผิด

### 4.2 เขียนไฟล์ข้อสอบ เช่น `e3.json`

```json
{
  "questionSets": [
    {
      "id": "cp-qset-1",
      "name": "แบบทดสอบ ป.วิ.แพ่ง ภาค 1",
      "category": "book",
      "refBookId": "civil-procedure-2477"
    }
  ],
  "questions": [
    {
      "id": "q-cp-001",
      "questionSetId": "cp-qset-1",
      "bookId": "civil-procedure-2477",
      "chapterId": "cp-ch1",
      "textMarkdown": "ข้อใดเป็น **เขตอำนาจศาล** ตามมาตรา 4",
      "imagePath": null,
      "explanationMarkdown": "ตาม **มาตรา 4** คำฟ้องให้เสนอต่อศาลที่จำเลยมีภูมิลำเนา...",
      "difficulty": "normal",
      "choices": [
        { "textMarkdown": "ศาลที่จำเลยมีภูมิลำเนาในเขต", "imagePath": null, "isCorrect": true },
        { "textMarkdown": "ศาลที่โจทก์มีภูมิลำเนาในเขตเท่านั้น", "imagePath": null, "isCorrect": false },
        { "textMarkdown": "ศาลใดก็ได้ที่โจทก์เลือก", "imagePath": null, "isCorrect": false },
        { "textMarkdown": "ศาลฎีกา", "imagePath": null, "isCorrect": false }
      ]
    }
  ]
}
```

#### ชุดข้อสอบ `questionSets[]`

| ฟิลด์ | จำเป็น | คำอธิบาย |
|---|:-:|---|
| `id` | ✅ | ห้ามซ้ำ ห้ามเปลี่ยนหลังเผยแพร่ |
| `name` | ✅ | ชื่อที่แสดง |
| `category` | ✅ | `book` = แสดงในแท็บ **"ตามหนังสือ"** (ต้องใส่ `refBookId`)<br>`custom` = แสดงในแท็บ **"ตามชุดคำถาม"** (`refBookId` เป็น `null`)<br>ค่าอื่นจะ**ไม่แสดงในแท็บไหนเลย** |
| `refBookId` | – | `id` ของหนังสือ — ต้องมีอยู่จริง |

#### คำถาม `questions[]`

| ฟิลด์ | จำเป็น | คำอธิบาย |
|---|:-:|---|
| `id` | ✅ | ห้ามซ้ำทั้ง repo แนะนำ `q-<ชุด>-<เลข 3 หลัก>` |
| `questionSetId` | ✅ | ต้องตรงกับ `questionSets[].id` (อยู่คนละไฟล์ได้) |
| `bookId`, `chapterId` | – | ผูกคำถามกับหนังสือ/บท — **ตอนนี้แอปยังไม่ได้ใช้** (โหมดทบทวนเลือกตาม `refBookId` ของชุดข้อสอบ) ใส่ไว้ได้เผื่ออนาคต หรือ `null` |
| `textMarkdown` | ✅ | โจทย์ |
| `imagePath` | – | รูปประกอบโจทย์ `image/xxx.png.enc` |
| `explanationMarkdown` | ✅ | คำอธิบายเฉลย แสดงหลังตอบ |
| `difficulty` | – | `easy` / `normal` / `hard` — ตอนนี้แอปเก็บไว้เฉยๆ ยังไม่ได้ใช้กรอง |
| `choices[]` | ✅ | ตัวเลือก — **ลำดับในไฟล์ = ลำดับที่แสดง** (แอปสุ่มลำดับ*คำถาม* แต่ไม่สุ่มตัวเลือก) → อย่าวางข้อถูกไว้ตำแหน่งเดิมทุกข้อ |
| `choices[].isCorrect` | ✅ | **ต้องมี `true` 1 ตัวพอดี** — แอปใช้ตัวแรกที่เจอเป็นเฉลย ถ้าไม่มีเลยจะไม่มีทางตอบถูก |
| `choices[].textMarkdown` / `imagePath` | ✅ อย่างใดอย่างหนึ่ง | ตัวเลือกเป็นข้อความ รูป หรือทั้งคู่ก็ได้ |

#### ตัวอย่างตัวเลือกเป็นรูป

```json
"choices": [
  { "textMarkdown": null, "imagePath": "image/topology-star.png.enc", "isCorrect": true },
  { "textMarkdown": null, "imagePath": "image/topology-ring.png.enc", "isCorrect": false }
]
```

### 4.3 การจัดรูปแบบข้อความในข้อสอบ

ใช้ได้ใน `textMarkdown` ของโจทย์ ตัวเลือก และ `explanationMarkdown`

| เขียน | แสดง |
|---|---|
| `**มาตรา 4**` | **มาตรา 4** |
| `*หมายเหตุ*` | *หมายเหตุ* |
| `$1011_2$` | 1011₂ |
| `$2^{6} - 2$` | 2⁶ − 2 |
| `$\\frac{64}{2}$` | เศษส่วนแบบซ้อนบนล่าง |
| `$\\sqrt{16}$` | รากที่สอง |

⚠️ **ใน JSON ต้องพิมพ์ backslash สองตัว** — `\\frac` ไม่ใช่ `\frac` (ตัวเดียว JSON จะอ่านผิดหรือพัง)

---

## 5. เพิ่มเส้นทางตามตำแหน่งงาน

แท็บ "ตามตำแหน่ง" — บอกผู้ใช้ว่าต้องอ่านบทไหน/สอบชุดไหน ตามลำดับ ใส่ไว้ในไฟล์ไหนก็ได้ (แนะนำแยก `p1.json`)

```json
{
  "positions": [
    {
      "id": "pos-court-officer",
      "name": "เจ้าพนักงานศาลยุติธรรม",
      "description": "อ่านและสอบตามลำดับนี้ก่อนสอบจริง",
      "requirements": [
        { "requiredType": "chapter",     "refId": "cp-ch1",    "orderIndex": 1 },
        { "requiredType": "questionset", "refId": "cp-qset-1", "orderIndex": 2 }
      ]
    }
  ]
}
```

| ฟิลด์ | คำอธิบาย |
|---|---|
| `requiredType` | `chapter` = ต้องอ่านบทนี้ / `questionset` = ต้องสอบชุดนี้ |
| `refId` | `id` ของบทหรือชุดข้อสอบ — **ต้องมีอยู่จริง** ไม่งั้นขั้นนั้นจะค้างไม่มีวันผ่าน |
| `orderIndex` | ลำดับขั้น เริ่ม 1 |

เกณฑ์ "ผ่าน" ชุดข้อสอบในเส้นทาง = **ตอบถูก 60% ขึ้นไป** (กำหนดในแอป)

> ⚠️ ไฟล์ที่มีแต่ `positions` ต้องมาหลังไฟล์บทเรียน/ข้อสอบที่มันอ้าง — เครื่องมือเรียงไฟล์ที่มี `books` ขึ้นก่อนให้แล้ว
> ส่วนไฟล์อื่นเรียงตามชื่อ ตั้งชื่อ `p1.json` จะมาหลัง `e*.json` อยู่แล้ว

---

## 6. เข้ารหัสและเผยแพร่

1. เปิด `QuizContentEncryptor.exe` → ใส่ **รหัสผ่าน**
2. **เวอร์ชัน / changeLog**
   - เวอร์ชัน: **เว้นว่างได้** เครื่องมือขึ้นเลขท้ายให้ (`1.0.3` → `1.0.4`)
   - changeLog: เขียนสั้นๆ ว่าเพิ่มอะไร — **ผู้ใช้เห็นข้อความนี้**ในหน้าดาวน์โหลด
3. **สร้างเนื้อหา + manifest** → เลือก **`.json` ทุกไฟล์พร้อมกัน** (Ctrl+A ในหน้าต่างเลือกไฟล์)
   - ⚠️ ต้องเลือก**ครบทุกไฟล์ทุกครั้ง** ไม่ใช่แค่ไฟล์ที่แก้ — manifest ใหม่จะมีเฉพาะไฟล์ที่เลือก ไฟล์ที่ไม่ได้เลือกจะหายจาก manifest
   - ไม่ต้องห่วงเรื่องโหลดซ้ำ: ไฟล์ที่ไม่ได้แก้ได้ sha256 เท่าเดิม แอปข้ามให้เอง
   - log ต้องขึ้น `OK x1.enc ... (books 4)` ครบทุกไฟล์ และ `OK manifest.json version ... -> ...`
4. **commit + push** → เลือก `D:\claude.ai\content-hub`
   - log ต้องจบด้วย `OK push ขึ้น git แล้ว`

เสร็จ — ผู้ใช้ที่เปิดแอปครั้งถัดไปจะเห็น "พบเนื้อหาใหม่"
(raw.githubusercontent.com แคชไว้ ~5 นาที ถ้าเช็คทันทีหลัง push อาจยังเห็นของเก่า)

---

## 7. ตรวจสอบก่อน push

- [ ] ทุก `.pdf.enc` / `.png.enc` ที่อ้างใน JSON **มีไฟล์อยู่จริง**ใน `book/` `image/` และสะกดชื่อตรงกัน (รวมวรรณยุกต์/เลขไทย/เว้นวรรค)
- [ ] ทุก `id` ไม่ซ้ำ และ id ที่เคยเผยแพร่แล้วไม่ได้ถูกเปลี่ยน
- [ ] ทุก `questionSetId`, `refBookId`, `chapterId`, `refId` ชี้ไปที่ของที่มีอยู่จริง
- [ ] ทุกข้อมี `isCorrect: true` **1 ตัวพอดี**
- [ ] สูตรใช้ `\\` สองตัว
- [ ] เลือก `.json` **ครบทุกไฟล์**ตอนกดสร้าง manifest
- [ ] ไม่มีไฟล์ `.json` (นอกจาก manifest) `.pdf` `.png` ใน `git status` — ถ้ามีแปลว่า `.gitignore` หาย **หยุดก่อน push**

ตรวจรูปแบบ JSON: เปิดไฟล์ใน VS Code — จุดที่ผิด (ลืมจุลภาค, วงเล็บไม่ครบ) จะขีดเส้นแดงให้

---

## 8. กฎที่ห้ามพลาด

| กฎ | ถ้าพลาด |
|---|---|
| **`version` ต้องเปลี่ยนทุกครั้งที่เผยแพร่** | แอปคิดว่าไม่มีอะไรใหม่ ไม่โหลดเลย (เครื่องมือขึ้นให้อัตโนมัติ — อย่าพิมพ์เลขเดิมทับ) |
| **รหัสผ่านเดียวกันทุกไฟล์** — JSON, PDF, รูป, และในแอป | ถอดรหัสไม่ได้ ผู้ใช้เห็น "แอปเวอร์ชันนี้ใช้กับเนื้อหาชุดนี้ไม่ได้" |
| **ห้ามเปลี่ยน `id` ที่เผยแพร่ไปแล้ว** | ผู้ใช้ได้หนังสือ/ข้อสอบซ้ำ 2 ชุด และประวัติคะแนนไม่ผูกกับชุดใหม่ |
| **ลบออกจาก JSON ไม่ได้ลบในเครื่องผู้ใช้** | ของที่ผู้ใช้โหลดไปแล้วยังอยู่ — ถ้าต้องการเลิกใช้ ให้เปลี่ยนชื่อเป็น `[เลิกใช้] ...` แทน |
| **แก้รูป/PDF ต้องตั้งชื่อไฟล์ใหม่** (เช่น `osi-diagram-v2.png`) แล้วแก้ path ใน JSON | แอปเก็บไฟล์ตามชื่อ ถ้าชื่อเดิมมีในเครื่องแล้วจะ**ไม่โหลดซ้ำ** — ผู้ใช้เก่าเห็นรูป/PDF เก่าตลอดไป |
| **ห้ามแก้ `.enc` หรือ sha256 ใน manifest ด้วยมือ** | checksum ไม่ตรง แอปปฏิเสธทั้งรอบ |
| **ห้าม push ไฟล์ต้นฉบับ** | เนื้อหาเปิดอ่านได้จาก GitHub ตรงๆ ไม่มีประโยชน์ที่เข้ารหัส (ลบใน commit ถัดไปก็ยังค้างใน history) |

---

## 9. แก้ปัญหา

| ผู้ใช้เห็นข้อความ / อาการ | สาเหตุ | แก้ |
|---|---|---|
| "เนื้อหาในเครื่องเป็นรุ่นล่าสุดแล้ว" ทั้งที่เพิ่ง push | ลืมขึ้น version / GitHub ยังแคชอยู่ | ดู `version` ใน manifest บน GitHub / รอ 5 นาที |
| "หาแหล่งเนื้อหาไม่เจอ" | repo เปลี่ยนชื่อ/ตั้งเป็น private / ลืม push `.enc` | repo ต้องเป็น public และชื่อไฟล์ตรงกับ manifest |
| "ไฟล์เนื้อหาบนเซิร์ฟเวอร์ไม่สมบูรณ์" | แก้ไฟล์หลังสร้าง manifest / JSON ผิดรูปแบบ | กด "สร้างเนื้อหา + manifest" ใหม่แล้ว push |
| "แอปเวอร์ชันนี้ใช้กับเนื้อหาชุดนี้ไม่ได้" | รหัสผ่านไม่ตรงกับในแอป | เข้ารหัสใหม่ด้วยรหัสที่ถูก |
| ข้อสอบมีแต่รูปหาย | สะกด `imagePath` ไม่ตรงชื่อไฟล์ / ลืมเข้ารหัสรูป | ใช้ปุ่ม "ทดสอบถอดรหัส" กับไฟล์ใน `image/` แล้วเทียบชื่อ |
| แก้รูปแล้วแต่ผู้ใช้ยังเห็นรูปเก่า | ใช้ชื่อไฟล์เดิม แอปไม่โหลดซ้ำ | ตั้งชื่อไฟล์ใหม่ + แก้ path ใน JSON |
| หนังสือไม่มีปุ่มเปิด PDF | `pdfFileName` สะกดผิด / ไฟล์ไม่มีใน `book/` | เหมือนข้างบน |
| ข้อสอบชุดใหม่ไม่ขึ้นในแท็บไหนเลย | `category` ไม่ใช่ `book` หรือ `custom` | แก้ category |
| ขั้นในเส้นทางตำแหน่งงานไม่มีวันผ่าน | `refId` ชี้ไปที่ id ที่ไม่มีอยู่ | แก้ refId |
| ปุ่มกรองหมวดหนังสือขึ้นซ้ำ 2 ปุ่ม | `category` สะกดไม่เหมือนกัน (เว้นวรรค/ภาษา) | ใช้ชื่อหมวดเดียวกันทุกเล่ม |

### ดูว่าในไฟล์ .enc บน repo มีอะไรอยู่

`QuizContentEncryptor.exe` → รหัสผ่าน → **+ เลือกไฟล์ .enc** → **ทดสอบถอดรหัส** (บอกชนิดและขนาด) หรือ **ถอดรหัสแล้วบันทึกไฟล์** (ได้ JSON/PDF/รูปต้นฉบับกลับมา)
